# Legacy Audio Test GB

Strict Alexa-hosted public-Git import package for English (GB).

Repository root intentionally contains only the documented skill-package layout plus this README:

- `lambda/index.js`
- `lambda/package.json`
- `skill-package/skill.json`
- `skill-package/interactionModels/custom/en-GB.json`
- `skill-package/assets/images/...`

There is deliberately no `ask-resources.json`.
